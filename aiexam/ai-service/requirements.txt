# ── Core (required) — works on Python 3.11/3.12/3.13 ─────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# ── AI Evaluation (required) ──────────────────────────────────────────────────
google-generativeai==0.7.2

# ── HTTP ─────────────────────────────────────────────────────────────────────
httpx==0.27.0
requests==2.32.3

# ── OCR (optional — install separately if on Python ≤ 3.12) ───────────────────
# Run: pip install --prefer-binary pillow easyocr pdf2image pypdf python-docx
# Pillow>=10.0.0
# easyocr>=1.7.0
# pdf2image>=1.17.0
# numpy>=1.26.0
pypdf==4.1.0
python-docx==1.1.0
